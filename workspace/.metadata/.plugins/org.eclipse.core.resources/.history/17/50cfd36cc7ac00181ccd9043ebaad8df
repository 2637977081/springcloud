eureka:
	instance:
        hostname: localhost
        serverPort: 8001
    client:
        serviceUrl:
            defaultZone: http://${eureka.instance.hostname}:${eureka.instance.serverPort}/eureka/
server:
    port: 8762